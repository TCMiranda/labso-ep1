#ifndef EVENT_LOOP_KEY_HEADERS
#define EVENT_LOOP_KEY_HEADERS

typedef enum {

  EXIT = -1,
  DEFAULT_ENTRY,
  PROCESSES_ENTRY,
  MEMORY_REQUEST,
  CPU_REQUEST,
  IO_REQUEST,
  IO_EXECUTION,
  IO_RELEASE,
  CPU_RELEASE

} event_loop_key;

#endif
